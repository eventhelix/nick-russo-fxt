<?xml version="1.0" encoding="utf-8" ?>
<!--
HTTP/1.x, HTTP/2, HTTP/3, QUIC, TLS, DNS, WCCP, JSON-RPC

Emoji story for HTTP lifecycle:
  🔗 TCP → 🤝 TLS Hello → 🔐 Key Exchange → 🔒 Encrypted →
  📡 HTTP GET/POST → ✅ 200 OK / ↩ 301 Redirect / 🚫 4xx / 💥 5xx

HTTP/3: 🔗 QUIC Initial → 🤝 QUIC Handshake → 📡 HTTP/3 → ✅ Response

Coverage:
  - HTTP/2 over TLS (with keylog decryption)
  - HTTP/3 over QUIC
  - HTTP/1.x (plain)
  - TLS 1.3 handshake
  - DNS A/AAAA resolution
  - WCCP (Web Cache Communication Protocol)
  - JSON-RPC over HTTP (Arista EOS eAPI)
-->
<FXT>

  <!-- ============================================================
       HTTP/2 REQUESTS (per-method emojis)
       ============================================================ -->

  <!-- HTTP/2 GET -->
  <tcp-message style="royal-blue" bookmark="true">
    <opcode regex-match="^:method: (GET)$" regex-replace="📡 HTTP/2 $1">http2.headers.method</opcode>
    <param regex-match=":authority: (.*)" regex-replace="🏠 Authority: $1">http2.headers.authority</param>
    <param regex-match=":path: (.*)" regex-replace="📂 Path: $1">http2.headers.path</param>
    <param regex-match=".*Stream Identifier: ([1-9]\d*)" regex-replace="🔢 Stream: $1">http2.streamid</param>
    <remark regex-match=".*" regex-replace="💡 HTTP/2 GET retrieves a resource — multiplexed over a single TCP connection via streams (no head-of-line blocking at application layer)">http2</remark>
  </tcp-message>

  <!-- HTTP/2 POST -->
  <tcp-message style="amethyst" bookmark="true">
    <opcode regex-match="^:method: (POST)$" regex-replace="📤 HTTP/2 $1">http2.headers.method</opcode>
    <param regex-match=":authority: (.*)" regex-replace="🏠 Authority: $1">http2.headers.authority</param>
    <param regex-match=":path: (.*)" regex-replace="📂 Path: $1">http2.headers.path</param>
    <param regex-match="content-type: (.*)" regex-replace="📦 Content-Type: $1">http2.headers.content_type</param>
    <param regex-match=".*Stream Identifier: ([1-9]\d*)" regex-replace="🔢 Stream: $1">http2.streamid</param>
    <remark regex-match=".*" regex-replace="💡 HTTP/2 POST sends data to server — request body follows in DATA frames on the same stream">http2</remark>
  </tcp-message>

  <!-- HTTP/2 PUT/PATCH/DELETE/OPTIONS/HEAD -->
  <tcp-message style="royal-blue dashed">
    <opcode regex-match="^:method: (.+)$" regex-replace="📡 HTTP/2 $1">http2.headers.method</opcode>
    <param regex-match=":authority: (.*)" regex-replace="🏠 Authority: $1">http2.headers.authority</param>
    <param regex-match=":path: (.*)" regex-replace="📂 Path: $1">http2.headers.path</param>
    <param regex-match=".*Stream Identifier: ([1-9]\d*)" regex-replace="🔢 Stream: $1">http2.streamid</param>
  </tcp-message>

  <!-- ============================================================
       HTTP/2 RESPONSES (per-status emojis)
       ============================================================ -->

  <!-- HTTP/2 301/302 Redirect -->
  <tcp-message style="amber" bookmark="true">
    <opcode regex-match="^:status: (3\d{2})$" regex-replace="↩ HTTP/2 $1 Redirect">http2.headers.status</opcode>
    <param regex-match="location: (.*)" regex-replace="📂 Location: $1">http2.headers.location</param>
    <param regex-match=".*Stream Identifier: ([1-9]\d*)" regex-replace="🔢 Stream: $1">http2.streamid</param>
    <param regex-match="Time since request: (.*)" regex-replace="⏱ $1">http2.time</param>
    <remark regex-match=".*" regex-replace="💡 Redirect — client must follow the Location header to the new URL (301=permanent, 302=temporary, 307=preserve method)">http2</remark>
  </tcp-message>

  <!-- HTTP/2 2xx Success -->
  <tcp-message style="forest-green">
    <opcode regex-match="^:status: (2\d{2})$" regex-replace="✅ HTTP/2 $1 OK">http2.headers.status</opcode>
    <param regex-match="content-type: (.*)" regex-replace="📦 Content-Type: $1">http2.headers.content_type</param>
    <param regex-match="content-length: (.*)" regex-replace="📦 Content-Length: $1">http2.headers.content_length</param>
    <param regex-match="server: (.*)" regex-replace="🖥 Server: $1">http2.headers.server</param>
    <param regex-match="cache-control: (.*)" regex-replace="💾 Cache: $1">http2.headers.cache_control</param>
    <param regex-match=".*Stream Identifier: ([1-9]\d*)" regex-replace="🔢 Stream: $1">http2.streamid</param>
    <param regex-match="Time since request: (.*)" regex-replace="⏱ $1">http2.time</param>
  </tcp-message>

  <!-- HTTP/2 4xx Client Error -->
  <tcp-message style="brick" bookmark="true">
    <opcode regex-match="^:status: (4\d{2})$" regex-replace="🚫 HTTP/2 $1 Client Error">http2.headers.status</opcode>
    <param regex-match="content-type: (.*)" regex-replace="📦 Content-Type: $1">http2.headers.content_type</param>
    <param regex-match=".*Stream Identifier: ([1-9]\d*)" regex-replace="🔢 Stream: $1">http2.streamid</param>
    <param regex-match="Time since request: (.*)" regex-replace="⏱ $1">http2.time</param>
    <remark regex-match=".*" regex-replace="💡 Client error — 400=bad request, 401=unauthorized, 403=forbidden, 404=not found">http2</remark>
  </tcp-message>

  <!-- HTTP/2 5xx Server Error -->
  <tcp-message style="brick wave" bookmark="true">
    <opcode regex-match="^:status: (5\d{2})$" regex-replace="💥 HTTP/2 $1 Server Error">http2.headers.status</opcode>
    <param regex-match="content-type: (.*)" regex-replace="📦 Content-Type: $1">http2.headers.content_type</param>
    <param regex-match=".*Stream Identifier: ([1-9]\d*)" regex-replace="🔢 Stream: $1">http2.streamid</param>
    <param regex-match="Time since request: (.*)" regex-replace="⏱ $1">http2.time</param>
    <remark regex-match=".*" regex-replace="💡 Server error — 500=internal error, 502=bad gateway, 503=service unavailable, 504=gateway timeout">http2</remark>
  </tcp-message>

  <!-- ============================================================
       HTTP/2 DATA & CONTROL FRAMES
       ============================================================ -->

  <!-- HTTP/2 DATA (end stream) -->
  <tcp-message style="teal">
    <opcode regex-match="^Stream: (DATA).*End Stream.*" regex-replace="📦 HTTP/2 $1 (End)">http2.stream</opcode>
    <param regex-match=".*Stream Identifier: ([1-9]\d*)" regex-replace="🔢 Stream: $1">http2.streamid</param>
  </tcp-message>

  <!-- HTTP/2 DATA -->
  <tcp-message style="teal dashed">
    <opcode regex-match="^Stream: (DATA).*" regex-replace="📦 HTTP/2 $1">http2.stream</opcode>
    <param regex-match=".*Stream Identifier: ([1-9]\d*)" regex-replace="🔢 Stream: $1">http2.streamid</param>
  </tcp-message>

  <!-- HTTP/2 SETTINGS -->
  <tcp-message style="slate dotted">
    <opcode regex-match="^Type: (SETTINGS).*" regex-replace="⚙ HTTP/2 $1">http2.type</opcode>
    <param regex-match=".*ACK: (True)" regex-replace="✅ ACK">http2.flags.ack.settings</param>
    <param regex-match="Initial Window Size: (.*)" regex-replace="📊 Window: $1">http2.settings.initial_window_size</param>
    <param regex-match="Max concurrent streams: (.*)" regex-replace="🔢 Max Streams: $1">http2.settings.max_concurrent_streams</param>
    <remark regex-match=".*" regex-replace="💡 HTTP/2 connection preface — peers exchange settings (max streams, window size, frame size) before any request">http2</remark>
  </tcp-message>

  <!-- HTTP/2 WINDOW_UPDATE -->
  <tcp-message style="slate dotted">
    <opcode regex-match="^Type: (WINDOW_UPDATE).*" regex-replace="📊 HTTP/2 $1">http2.type</opcode>
    <param regex-match=".*Window Size Increment: (\d+)" regex-replace="📊 Increment: $1">http2.window_update.window_size_increment</param>
    <param regex-match=".*Stream Identifier: ([1-9]\d*)" regex-replace="🔢 Stream: $1">http2.streamid</param>
  </tcp-message>

  <!-- HTTP/2 PRIORITY -->
  <tcp-message style="slate dotted">
    <opcode regex-match="^Type: (PRIORITY).*" regex-replace="📊 HTTP/2 $1">http2.type</opcode>
    <param regex-match=".*Stream Identifier: ([1-9]\d*)" regex-replace="🔢 Stream: $1">http2.streamid</param>
  </tcp-message>

  <!-- HTTP/2 PING -->
  <tcp-message style="slate dotted">
    <opcode regex-match="^Type: (PING).*" regex-replace="💓 HTTP/2 $1">http2.type</opcode>
    <param regex-match=".*ACK: (True)" regex-replace="✅ ACK">http2.flags.ack.ping</param>
  </tcp-message>

  <!-- HTTP/2 GOAWAY -->
  <tcp-message style="brick" bookmark="true">
    <opcode regex-match="^Type: (GOAWAY).*" regex-replace="🛑 HTTP/2 $1">http2.type</opcode>
    <param>http2.goaway.last_stream_id</param>
    <param>http2.goaway.error</param>
    <remark regex-match=".*" regex-replace="💡 Server gracefully shuts down — last_stream_id tells client which requests were processed; client must retry higher-numbered streams on a new connection">http2</remark>
  </tcp-message>

  <!-- HTTP/2 RST_STREAM -->
  <tcp-message style="brick wave">
    <opcode regex-match="^Type: (RST_STREAM).*" regex-replace="❌ HTTP/2 $1">http2.type</opcode>
    <param regex-match="Error: (.*)" regex-replace="❌ $1">http2.rst_stream.error</param>
    <param regex-match=".*Stream Identifier: ([1-9]\d*)" regex-replace="🔢 Stream: $1">http2.streamid</param>
    <remark regex-match=".*" regex-replace="💡 Abruptly terminates a single stream without closing the TCP connection — other streams continue unaffected">http2</remark>
  </tcp-message>

  <!-- HTTP/2 HEADERS fallback -->
  <tcp-message style="steel-blue dashed">
    <opcode regex-match="^Stream: (HEADERS).*" regex-replace="🔗 HTTP/2 $1">http2.stream</opcode>
    <param regex-match=":status: (.*)" regex-replace="📋 Status: $1">http2.headers.status</param>
    <param regex-match=".*Stream Identifier: ([1-9]\d*)" regex-replace="🔢 Stream: $1">http2.streamid</param>
  </tcp-message>

  <!-- Generic HTTP/2 frame fallback -->
  <tcp-message style="slate dotted">
    <opcode regex-match="^Type: (.+) \(.*\)" regex-replace="🔗 HTTP/2 $1">http2.type</opcode>
    <param regex-match=".*Stream Identifier: ([1-9]\d*)" regex-replace="🔢 Stream: $1">http2.streamid</param>
  </tcp-message>

  <!-- ============================================================
       DNS MESSAGES
       ============================================================ -->

  <!-- DNS Query A -->
  <udp-message style="sky-blue">
    <opcode regex-match="^Type: (A) .*" regex-replace="🔍 DNS Query A">dns.qry.type</opcode>
    <param regex-match="Name: (.*)" regex-replace="🌐 $1">dns.qry.name</param>
    <remark regex-match=".*" regex-replace="💡 DNS A record query — resolve hostname to IPv4 address (first step before any HTTP connection)">dns</remark>
  </udp-message>

  <!-- DNS Query AAAA -->
  <udp-message style="cerulean">
    <opcode regex-match="^Type: (AAAA) .*" regex-replace="🔍 DNS Query AAAA">dns.qry.type</opcode>
    <param regex-match="Name: (.*)" regex-replace="🌐 $1">dns.qry.name</param>
    <remark regex-match=".*" regex-replace="💡 DNS AAAA record query — resolve hostname to IPv6 address">dns</remark>
  </udp-message>

  <!-- DNS Response A -->
  <udp-message style="sea-green">
    <opcode regex-match="^Address: (.+)" regex-replace="📋 DNS A: $1">dns.a</opcode>
    <param regex-match="Name: (.*)" regex-replace="🌐 $1">dns.resp.name</param>
    <param regex-match="Time to live: (.*)" regex-replace="⏱ TTL: $1">dns.resp.ttl</param>
    <param regex-match="Time: (.*)" regex-replace="⏱ $1">dns.time</param>
  </udp-message>

  <!-- DNS Response AAAA -->
  <udp-message style="sea-green">
    <opcode regex-match="^AAAA Address: (.+)" regex-replace="📋 DNS AAAA: $1">dns.aaaa</opcode>
    <param regex-match="Name: (.*)" regex-replace="🌐 $1">dns.resp.name</param>
    <param regex-match="Time to live: (.*)" regex-replace="⏱ TTL: $1">dns.resp.ttl</param>
    <param regex-match="Time: (.*)" regex-replace="⏱ $1">dns.time</param>
  </udp-message>

  <!-- DNS Response generic -->
  <udp-message style="sea-green">
    <opcode regex-match=".*Message is a (response)" regex-replace="📋 DNS Response">dns.flags.response</opcode>
    <param regex-match="Name: (.*)" regex-replace="🌐 $1">dns.resp.name</param>
    <param regex-match="Time: (.*)" regex-replace="⏱ $1">dns.time</param>
  </udp-message>

  <!-- ============================================================
       HTTP/3 REQUESTS (over QUIC — visible with keylog decryption)
       ============================================================ -->

  <!-- HTTP/3 GET -->
  <udp-message style="royal-blue" bookmark="true">
    <opcode regex-match="^:method: (GET)$" regex-replace="📡 HTTP/3 $1">http3.headers.method</opcode>
    <param regex-match=":authority: (.*)" regex-replace="🏠 Authority: $1">http3.headers.authority</param>
    <param regex-match=":path: (.*)" regex-replace="📂 Path: $1">http3.headers.path</param>
    <remark regex-match=".*" regex-replace="💡 HTTP/3 GET over QUIC — each request uses an independent stream, so one slow response can't block others (no head-of-line blocking at any layer)">http3</remark>
  </udp-message>

  <!-- HTTP/3 POST -->
  <udp-message style="amethyst" bookmark="true">
    <opcode regex-match="^:method: (POST)$" regex-replace="📤 HTTP/3 $1">http3.headers.method</opcode>
    <param regex-match=":authority: (.*)" regex-replace="🏠 Authority: $1">http3.headers.authority</param>
    <param regex-match=":path: (.*)" regex-replace="📂 Path: $1">http3.headers.path</param>
    <param regex-match="content-type: (.*)" regex-replace="📦 Content-Type: $1">http3.headers.content_type</param>
  </udp-message>

  <!-- HTTP/3 Other Methods -->
  <udp-message style="royal-blue dashed">
    <opcode regex-match="^:method: (.+)$" regex-replace="📡 HTTP/3 $1">http3.headers.method</opcode>
    <param regex-match=":authority: (.*)" regex-replace="🏠 Authority: $1">http3.headers.authority</param>
    <param regex-match=":path: (.*)" regex-replace="📂 Path: $1">http3.headers.path</param>
  </udp-message>

  <!-- ============================================================
       HTTP/3 RESPONSES
       ============================================================ -->

  <!-- HTTP/3 200 OK -->
  <udp-message style="forest-green">
    <opcode regex-match="^:status: (200)$" regex-replace="✅ HTTP/3 $1 OK">http3.headers.status</opcode>
    <param regex-match="content-type: (.*)" regex-replace="📦 Content-Type: $1">http3.headers.content_type</param>
    <param regex-match="content-length: (.*)" regex-replace="📦 Length: $1">http3.headers.content_length</param>
    <param regex-match="server: (.*)" regex-replace="🖥 Server: $1">http3.headers.server</param>
    <param regex-match="cache-control: (.*)" regex-replace="💾 Cache: $1">http3.headers.cache_control</param>
  </udp-message>

  <!-- HTTP/3 3xx Redirect -->
  <udp-message style="amber">
    <opcode regex-match="^:status: (3\d{2})$" regex-replace="↩ HTTP/3 $1 Redirect">http3.headers.status</opcode>
  </udp-message>

  <!-- HTTP/3 4xx/5xx Error -->
  <udp-message style="brick">
    <opcode regex-match="^:status: ([45]\d{2})$" regex-replace="🚫 HTTP/3 $1 Error">http3.headers.status</opcode>
  </udp-message>

  <!-- ============================================================
       HTTP/3 CONTROL FRAMES
       ============================================================ -->

  <!-- HTTP/3 SETTINGS -->
  <udp-message style="coral">
    <opcode regex-match="^Type: (SETTINGS).*" regex-replace="⚙ HTTP/3 $1">http3.frame_type</opcode>
    <remark regex-match=".*" regex-replace="💡 HTTP/3 settings exchange — peers declare QPACK table capacity, max concurrent streams; unlike HTTP/2, this uses a dedicated unidirectional control stream">http3</remark>
  </udp-message>

  <!-- HTTP/3 HEADERS frame (fallback when method/status not decoded) -->
  <udp-message style="coral dashed">
    <opcode regex-match="^(HEADERS).*len=(\d+).*" regex-replace="🔗 HTTP/3 $1 ($2 bytes)">http3.frame</opcode>
  </udp-message>

  <!-- HTTP/3 DATA frame -->
  <udp-message style="teal">
    <opcode regex-match="^(DATA).*len=(\d+).*" regex-replace="📦 HTTP/3 $1 ($2 bytes)">http3.frame</opcode>
    <remark regex-match=".*" regex-replace="💡 HTTP/3 DATA frame carries the response body — transmitted on the same QUIC stream as the HEADERS, independently of other requests">http3</remark>
  </udp-message>

  <!-- HTTP/3 Uni Stream (Control/QPACK Encoder/Decoder) -->
  <udp-message style="coral dotted">
    <opcode regex-match="^UNI STREAM: (Control Stream).*" regex-replace="⚙ HTTP/3 $1">http3.stream.uni</opcode>
    <remark regex-match=".*" regex-replace="💡 HTTP/3 unidirectional control stream — carries SETTINGS and GOAWAY frames; each peer creates exactly one">http3</remark>
  </udp-message>

  <udp-message style="orchid dotted">
    <opcode regex-match="^UNI STREAM: (QPACK Encoder).*" regex-replace="🗜 HTTP/3 $1">http3.stream.uni</opcode>
    <remark regex-match=".*" regex-replace="💡 QPACK encoder stream — sends dynamic table updates for header compression (replaces HTTP/2 HPACK with out-of-order safe design)">http3</remark>
  </udp-message>

  <udp-message style="orchid dotted">
    <opcode regex-match="^UNI STREAM: (QPACK Decoder).*" regex-replace="🗜 HTTP/3 $1">http3.stream.uni</opcode>
    <remark regex-match=".*" regex-replace="💡 QPACK decoder stream — acknowledges received header table entries so encoder can evict safely">http3</remark>
  </udp-message>

  <!-- HTTP/3 Generic Uni Stream -->
  <udp-message style="coral dotted">
    <opcode regex-match="^UNI STREAM: (.+) off=.*" regex-replace="📡 HTTP/3 $1">http3.stream.uni</opcode>
  </udp-message>

  <!-- HTTP/3 Generic Frame -->
  <udp-message style="coral dotted">
    <opcode regex-match="^(SETTINGS|Reserved).*len=(\d+)" regex-replace="📡 HTTP/3 $1 ($2 bytes)">http3.frame</opcode>
  </udp-message>

  <!-- ============================================================
       QUIC TRANSPORT
       Match order: TLS handshake → QUIC frame type → packet type fallback
       ============================================================ -->

  <!-- QUIC Client Hello (TLS inside Initial) -->
  <udp-message style="tangerine" bookmark="true">
    <opcode regex-match=".*Client Hello.*" regex-replace="🤝 QUIC Client Hello">tls.handshake.type</opcode>
    <param regex-match="Server Name: (.*)" regex-replace="🌐 SNI: $1">tls.handshake.extensions_server_name</param>
    <param regex-match=".*ALPN.*: (h3.*)" regex-replace="📡 ALPN: $1">tls.handshake.extensions_alpn_str</param>
    <param regex-match="Version: (.*)" regex-replace="📋 QUIC: $1">quic.version</param>
    <remark regex-match=".*" regex-replace="💡 QUIC Initial carries TLS 1.3 ClientHello — SNI selects server certificate; ALPN negotiates h3; connection IDs enable seamless path migration (e.g., Wi-Fi to cellular)">quic</remark>
  </udp-message>

  <!-- QUIC Server Hello (TLS inside Initial/Handshake) -->
  <udp-message style="sea-green" bookmark="true">
    <opcode regex-match=".*Server Hello.*" regex-replace="🤝 QUIC Server Hello">tls.handshake.type</opcode>
    <param regex-match="Cipher Suite: (.*)" regex-replace="🔐 Cipher: $1">tls.handshake.ciphersuite</param>
    <param regex-match="Version: (.*)" regex-replace="📋 QUIC: $1">quic.version</param>
    <remark regex-match=".*" regex-replace="💡 Server selects TLS 1.3 cipher — QUIC completes handshake in 1-RTT (vs TCP+TLS 2-3 RTT); returning clients can send 0-RTT data immediately">quic</remark>
  </udp-message>

  <!-- QUIC CRYPTO frame -->
  <udp-message style="amethyst">
    <opcode regex-match="^Frame Type: (CRYPTO).*" regex-replace="🔐 QUIC CRYPTO">quic.frame_type</opcode>
    <param regex-match="Offset: (.*)" regex-replace="📋 Offset: $1">quic.crypto.offset</param>
    <param regex-match="Version: (.*)" regex-replace="📋 QUIC: $1">quic.version</param>
    <remark regex-match=".*" regex-replace="💡 TLS handshake data (Certificate chain, CertificateVerify, Finished) carried in QUIC CRYPTO frames — encrypted at handshake level, decoded with keylog">quic</remark>
  </udp-message>

  <!-- QUIC HANDSHAKE_DONE -->
  <udp-message style="forest-green" bookmark="true">
    <opcode regex-match="^Frame Type: (HANDSHAKE_DONE).*" regex-replace="✅ QUIC Handshake Done">quic.frame_type</opcode>
    <remark regex-match=".*" regex-replace="💡 Server confirms handshake complete — client can now discard handshake keys and send 1-RTT application data; the QUIC connection is fully established">quic</remark>
  </udp-message>

  <!-- QUIC ACK -->
  <udp-message style="slate dotted">
    <opcode regex-match="^Frame Type: (ACK).*" regex-replace="✅ QUIC ACK">quic.frame_type</opcode>
    <param regex-match="Largest Acknowledged: (.*)" regex-replace="📋 Largest ACK: $1">quic.ack.largest_acknowledged</param>
  </udp-message>

  <!-- QUIC NEW_CONNECTION_ID -->
  <udp-message style="cerulean">
    <opcode regex-match="^Frame Type: (NEW_CONNECTION_ID).*" regex-replace="🔄 QUIC New Connection ID">quic.frame_type</opcode>
    <remark regex-match=".*" regex-replace="💡 Server provides alternative connection IDs — enables connection migration across network changes (Wi-Fi ↔ cellular) without re-handshaking">quic</remark>
  </udp-message>

  <!-- QUIC Initial fallback -->
  <udp-message style="tangerine dashed">
    <opcode regex-match=".*Packet Type: (Initial).*" regex-replace="🔗 QUIC $1">quic.long.packet_type</opcode>
    <param regex-match="Version: (.*)" regex-replace="📋 QUIC: $1">quic.version</param>
    <remark regex-match=".*" regex-replace="💡 QUIC Initial packet — carries ClientHello (client→server) or ServerHello+ACK (server→client); padded to 1200+ bytes to prevent amplification attacks">quic</remark>
  </udp-message>

  <!-- QUIC Handshake fallback -->
  <udp-message style="cerulean dashed">
    <opcode regex-match=".*Packet Type: (Handshake).*" regex-replace="🔐 QUIC $1">quic.long.packet_type</opcode>
    <param regex-match="Version: (.*)" regex-replace="📋 QUIC: $1">quic.version</param>
    <remark regex-match=".*" regex-replace="💡 QUIC Handshake packet — carries encrypted TLS data (Certificate, Finished) in CRYPTO frames; uses handshake-level encryption keys">quic</remark>
  </udp-message>

  <!-- QUIC Short Header with STREAM info -->
  <udp-message style="teal">
    <opcode regex-match="^(STREAM) id=(\d+).*len=(\d+).*" regex-replace="📦 QUIC $1 id=$2 ($3 bytes)">quic.frame</opcode>
    <param regex-match="^(STREAM) id=(\d+).*(fin=1).*" regex-replace="🏁 Final">quic.frame</param>
  </udp-message>

  <!-- QUIC Short Header fallback -->
  <udp-message style="royal-blue dashed">
    <opcode regex-match="^QUIC Short Header DCID=.* PKN=(\d+)" regex-replace="📦 QUIC Data PKN=$1">quic.short</opcode>
    <param regex-match="^(STREAM) id=(\d+).*" regex-replace="📦 Stream: $2">quic.frame</param>
  </udp-message>

  <!-- QUIC Generic frame -->
  <udp-message style="goldenrod dotted">
    <opcode regex-match="^(CRYPTO|ACK|PADDING|STREAM|NEW_CONNECTION_ID|HANDSHAKE_DONE).*" regex-replace="🔗 QUIC $1">quic.frame</opcode>
  </udp-message>

  <!-- ============================================================
       TLS HANDSHAKE
       ============================================================ -->

  <!-- TLS Client Hello -->
  <tcp-message style="plum dashed" bookmark="true">
    <opcode regex-match=".*Client Hello.*" regex-replace="🤝 TLS Client Hello">tls.handshake.type</opcode>
    <param regex-match="Server Name: (.*)" regex-replace="🌐 SNI: $1">tls.handshake.extensions_server_name</param>
    <param regex-match=".*ALPN.*: (h2|http/1\.1)" regex-replace="📡 ALPN: $1">tls.handshake.extensions_alpn_str</param>
    <param regex-match="TLS (1\.[23]).*" regex-replace="🔐 TLS $1">tls.handshake.extensions_supported_version</param>
    <remark regex-match=".*" regex-replace="💡 TLS Client Hello initiates the handshake — SNI tells the server which certificate to use (required for virtual hosting); ALPN negotiates h2 vs http/1.1">tls</remark>
  </tcp-message>

  <!-- TLS Server Hello -->
  <tcp-message style="teal" bookmark="true">
    <opcode regex-match=".*Server Hello.*" regex-replace="🤝 TLS Server Hello">tls.handshake.type</opcode>
    <param regex-match="Cipher Suite: (.*)" regex-replace="🔐 Cipher: $1">tls.handshake.ciphersuite</param>
    <param regex-match=".*Supported Version: TLS (1\.[23]).*" regex-replace="🔐 TLS $1">tls.handshake.extensions_supported_version</param>
    <remark regex-match=".*" regex-replace="💡 Server selects TLS version and cipher suite — TLS 1.3 completes in 1-RTT (vs 2-RTT for TLS 1.2) with forward secrecy via ephemeral key exchange">tls</remark>
  </tcp-message>

  <!-- TLS Certificate -->
  <tcp-message style="amethyst dashed">
    <opcode regex-match=".*Certificate.*" regex-replace="🔐 TLS Certificate">tls.handshake.type</opcode>
    <remark regex-match=".*" regex-replace="💡 Server presents X.509 certificate chain — client validates against trusted CAs to authenticate the server's identity">tls</remark>
  </tcp-message>

  <!-- TLS Other Handshake -->
  <tcp-message style="plum dashed">
    <opcode regex-match="Handshake Type: (.*) \(.*\)" regex-replace="🔐 TLS $1">tls.handshake.type</opcode>
  </tcp-message>

  <!-- TLS Change Cipher Spec -->
  <tcp-message style="plum dashed">
    <opcode regex-match=".*Change Cipher Spec.*" regex-replace="🔒 TLS Change Cipher Spec">tls.change_cipher_spec</opcode>
    <remark regex-match=".*" regex-replace="💡 Signals switch to negotiated cipher — all subsequent data encrypted (TLS 1.3 sends this for backwards compatibility only)">tls</remark>
  </tcp-message>

  <!-- TLS Application Data -->
  <tcp-message style="slate dashed">
    <opcode regex-match=".*" regex-replace="🔒 TLS Application Data">tls.app_data_proto</opcode>
  </tcp-message>

  <!-- ============================================================
       HTTP/1.x MESSAGES
       ============================================================ -->

  <!-- HTTP/1.x GET -->
  <tcp-message style="royal-blue" bookmark="true">
    <opcode regex-match="(GET)" regex-replace="📡 HTTP $1">http.request.method</opcode>
    <param display="brief" regex-match="(.*)" regex-replace="📂 URI: $1">http.request.uri</param>
    <param display="brief" regex-match="(.*)" regex-replace="🏠 Host: $1">http.host</param>
    <remark regex-match=".*" regex-replace="💡 HTTP/1.1 GET — unlike HTTP/2, requests are serialized per connection (head-of-line blocking); browsers open 6 parallel connections as workaround">http</remark>
  </tcp-message>

  <!-- HTTP/1.x POST -->
  <tcp-message style="amethyst" bookmark="true">
    <opcode regex-match="(POST)" regex-replace="📤 HTTP $1">http.request.method</opcode>
    <param display="brief" regex-match="(.*)" regex-replace="📂 URI: $1">http.request.uri</param>
    <param display="brief" regex-match="(.*)" regex-replace="🏠 Host: $1">http.host</param>
    <param display="brief" regex-match="(.*)" regex-replace="📦 Content-Type: $1">http.content_type</param>
  </tcp-message>

  <!-- HTTP/1.x Other Methods -->
  <tcp-message style="royal-blue dashed">
    <opcode regex-match="(.*)" regex-replace="📡 HTTP $1">http.request.method</opcode>
    <param display="brief" regex-match="(.*)" regex-replace="📂 URI: $1">http.request.uri</param>
    <param display="brief" regex-match="(.*)" regex-replace="🏠 Host: $1">http.host</param>
  </tcp-message>

  <!-- HTTP/1.x 200 OK -->
  <tcp-message style="forest-green">
    <opcode regex-match="(200)" regex-replace="✅ HTTP $1 OK">http.response.code</opcode>
    <param display="brief" regex-match="(.*)" regex-replace="📦 Content-Type: $1">http.content_type</param>
    <param display="brief" regex-match="(.*)" regex-replace="📦 Content-Length: $1">http.content_length</param>
    <param display="brief" regex-match="(.*)" regex-replace="🖥 Server: $1">http.server</param>
    <param regex-match="Time since request: (.*)" regex-replace="⏱ $1">http.time</param>
  </tcp-message>

  <!-- HTTP/1.x 3xx Redirect -->
  <tcp-message style="amber">
    <opcode regex-match="(3\d\d)" regex-replace="↩ HTTP $1 Redirect">http.response.code</opcode>
    <param display="brief" regex-match="(.*)" regex-replace="📋 $1">http.response.phrase</param>
    <param regex-match="Time since request: (.*)" regex-replace="⏱ $1">http.time</param>
  </tcp-message>

  <!-- HTTP/1.x 4xx/5xx Error -->
  <tcp-message style="brick" bookmark="true">
    <opcode regex-match="([45]\d\d)" regex-replace="🚫 HTTP $1 Error">http.response.code</opcode>
    <param display="brief" regex-match="(.*)" regex-replace="📋 $1">http.response.phrase</param>
    <param regex-match="Time since request: (.*)" regex-replace="⏱ $1">http.time</param>
  </tcp-message>

  <!-- HTTP/1.x Generic Response -->
  <tcp-message style="forest-green dashed">
    <opcode regex-match="(\d+)" regex-replace="📋 HTTP $1">http.response.code</opcode>
    <param display="brief" regex-match="(.*)" regex-replace="📋 $1">http.response.phrase</param>
    <param regex-match="Time since request: (.*)" regex-replace="⏱ $1">http.time</param>
  </tcp-message>

  <!-- ============================================================
       WCCP (Web Cache Communication Protocol)
       ============================================================ -->

  <!-- WCCP Here I Am -->
  <udp-message style="goldenrod" bookmark="true">
    <opcode regex-match=".*Here I am.*" regex-replace="👋 WCCP Here I Am">wccp.message</opcode>
    <param regex-match=".*: (10\..*)" regex-replace="🖥 Web Cache: $1">wccp.wc_identity_ip_address.ipv4</param>
    <param regex-match="Standard.*service \((\d+)\)" regex-replace="📋 Service: $1">wccp.service_info_type</param>
    <remark regex-match=".*" regex-replace="💡 Web cache announces itself to router — WCCP enables transparent HTTP redirection so clients don't need proxy configuration">wccp</remark>
  </udp-message>

  <!-- WCCP I See You -->
  <udp-message style="tangerine">
    <opcode regex-match=".*I see you.*" regex-replace="👀 WCCP I See You">wccp.message</opcode>
    <param regex-match=".*: (10\..*)" regex-replace="🖥 Router: $1">wccp.router_identity.ip_address.ipv4</param>
    <param regex-match="Receive ID: (.*)" regex-replace="🔢 Receive ID: $1">wccp.router_identity.receive_id</param>
    <remark regex-match=".*" regex-replace="💡 Router acknowledges web cache — includes member list so all caches know the cluster topology">wccp</remark>
  </udp-message>

  <!-- WCCP Redirect Assign -->
  <udp-message style="sea-green" bookmark="true">
    <opcode regex-match=".*Redirect assign.*" regex-replace="🔀 WCCP Redirect Assign">wccp.message</opcode>
    <param regex-match=".*: (Mask|Hash).*" regex-replace="📋 Assignment: $1">wccp.alt_assignment_info.assignment_type</param>
    <param regex-match=".*: (Supported)" regex-replace="🔗 GRE: $1">wccp.capability_info.forwarding_method_flag.gre</param>
    <remark regex-match=".*" regex-replace="💡 Web cache tells router how to distribute traffic — mask assignment maps client IP ranges to specific caches; GRE or L2 forwarding method">wccp</remark>
  </udp-message>

  <!-- WCCP Removal Query -->
  <udp-message style="brick">
    <opcode regex-match=".*Removal query.*" regex-replace="🗑 WCCP Removal Query">wccp.message</opcode>
    <remark regex-match=".*" regex-replace="💡 Router asks web cache to confirm removal from the service group — cache must respond or be forcibly removed">wccp</remark>
  </udp-message>

  <!-- WCCP Generic -->
  <udp-message style="goldenrod dashed">
    <opcode regex-match=".*" regex-replace="📡 WCCP">wccp.message</opcode>
    <param>wccp.message</param>
  </udp-message>

  <!-- ============================================================
       TCP TRANSPORT (SYN/FIN/RST only)
       ============================================================ -->

  <tcp-message style="graphite dotted">
    <opcode regex-match=".*Flags:.*\((SYN.*)\)" regex-replace="🔗 TCP ($1)">tcp.flags</opcode>
  </tcp-message>

  <tcp-message style="graphite dotted">
    <opcode regex-match=".*Flags:.*\((.*FIN.*)\)" regex-replace="🔗 TCP ($1)">tcp.flags</opcode>
  </tcp-message>

  <tcp-message style="crimson dotted">
    <opcode regex-match=".*Flags:.*\((.*RST.*)\)" regex-replace="🔗 TCP ($1)">tcp.flags</opcode>
  </tcp-message>

</FXT>
